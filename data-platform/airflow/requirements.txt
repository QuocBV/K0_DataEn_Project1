# SSI Trading Analytics - Airflow Dependencies
# Pinned versions for reproducible builds
# Install: pip install -r requirements.txt

# Airflow providers
apache-airflow-providers-snowflake==5.6.0
apache-airflow-providers-http==4.13.0

# dbt Core via Cosmos (orchestrate dbt from Airflow)
astronomer-cosmos==1.9.0

# Soda Core for data quality checks in Airflow
soda-core==3.4.0
soda-core-snowflake==3.4.0

# AWS SDK for S3 interactions
boto3==1.35.0

# Utilities
pyyaml==6.0.2
requests==2.32.3