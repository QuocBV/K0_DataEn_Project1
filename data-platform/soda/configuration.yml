# Soda configuration for Spark SQL (Iceberg lakehouse)
# Soda connects via a SparkSession (created in run_scan.py) reading Iceberg tables on S3.
data_source spark_ds:
  type: spark
  spark_session: spark
