# Trino catalog reading Airbyte raw Parquet on s3://ssi-data/raw/
# AWS credentials are picked up from container env (AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY / AWS_REGION)
connector.name=hive
hive.metastore.uri=thrift://hive-metastore:9083